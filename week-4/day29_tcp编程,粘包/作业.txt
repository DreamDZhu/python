TCP : 传输小文件，传输大文件
TCP : 基于tcp 协议完成登录认证
    # 客户端输入用户名密码
    # 发送到服务端
    # 服务端认证
    # 发送结果到客户端


#2. 基于udp 协议的多人聊天
    # 自动识别用户 不能用ip 和 Port


#3. 基于tcp 协议 完成一个文件的上传
    # 小文件
    # 进阶大文件

UDP : 可以多个人给你发消息，但是你可以识别记录每个人是谁 ，当不同的人发消息前，可以用不同颜色区分每个不同的人。（不能用ip地址来判断）
    进阶需求： 一个人跟多个人聊天 ，也就是server 端是一个中间件。
    udp 实现的多人聊天室






















